platform: ${platform}

clone:
  git:
    image: ${git}
    depth: 50

pipeline:
  build:
    image: ${container}
    #secrets: [AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY]
    commands:
      - ./bootstrap/bootstrap.sh ${distro}
    #  - VERSION=$(date +%y%m%d)
    #  - ./deps.sh
    #  - ./build.sh $VERSION
    #  - ./unit-test.sh
      #- ./upload.sh $DRONE_BRANCH $VERSION
    privileged: true
  
matrix:
  include:
    - platform: linux/arm
      container: armv7/armhf-debian:8
      git: syncloud/armhf-drone-git
      distro: debian
    - platform: linux/amd64
      container: debian:8
      git: plugins/git
      distro: amd64
